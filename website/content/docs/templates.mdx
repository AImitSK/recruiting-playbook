---
title: Templates
description: So ueberschreiben Sie die Plugin-Templates in Ihrem Theme.
---

Recruiting Playbook verwendet PHP-Templates fuer die Darstellung von Stellenanzeigen im Frontend. Diese Templates koennen in Ihrem Theme ueberschrieben werden, um das Aussehen an Ihr Design anzupassen.

## Template-Ueberschreibung

### Schritt 1: Ordner erstellen

Erstellen Sie im Root-Verzeichnis Ihres Themes (oder Child-Themes) den Ordner `recruiting-playbook/`:

```bash
wp-content/themes/ihr-theme/recruiting-playbook/
```

### Schritt 2: Templates kopieren

Kopieren Sie die gewuenschten Templates aus dem Plugin-Ordner:

```bash
# Quelle (Plugin)
wp-content/plugins/recruiting-playbook/templates/

# Ziel (Theme)
wp-content/themes/ihr-theme/recruiting-playbook/
```

### Schritt 3: Anpassen

Bearbeiten Sie die kopierten Dateien nach Ihren Wuenschen. Das Plugin laedt automatisch die Theme-Version, wenn sie vorhanden ist.

## Verfuegbare Templates

### Haupttemplates

| Template | Beschreibung |
|----------|-------------|
| `archive-job_listing.php` | Stellenliste (Archiv-Seite) |
| `single-job_listing.php` | Einzelne Stellenanzeige |

### Partials

| Template | Beschreibung |
|----------|-------------|
| `partials/job-card.php` | Karte fuer eine Stelle in der Liste |
| `partials/job-finder.php` | KI-Job-Finder Komponente (AI-Addon) |
| `partials/match-modal.php` | KI-Match Modal Dialog (AI-Addon) |

### Formular-Feld-Templates

Diese Templates steuern die Darstellung einzelner Formularfelder:

| Template | Beschreibung |
|----------|-------------|
| `fields/field-text.php` | Textfeld |
| `fields/field-email.php` | E-Mail-Feld |
| `fields/field-phone.php` | Telefon-Feld |
| `fields/field-url.php` | URL-Feld |
| `fields/field-number.php` | Zahlenfeld |
| `fields/field-date.php` | Datumsfeld |
| `fields/field-textarea.php` | Mehrzeiliges Textfeld |
| `fields/field-select.php` | Dropdown-Auswahl |
| `fields/field-radio.php` | Radio-Buttons |
| `fields/field-checkbox.php` | Checkboxen |
| `fields/field-file.php` | Datei-Upload |
| `fields/field-heading.php` | Ueberschrift (nicht editierbar) |
| `fields/field-html.php` | HTML-Block (nicht editierbar) |

### E-Mail-Templates

| Template | Zweck |
|----------|------|
| `emails/base-layout.php` | Basis-Layout fuer alle E-Mails |
| `emails/partials/header.php` | E-Mail-Kopfbereich |
| `emails/partials/footer.php` | E-Mail-Fussbereich |
| `emails/application-received.php` | Eingangsbestaetigung fuer Bewerber |
| `emails/hr-new-application.php` | Benachrichtigung an HR bei neuer Bewerbung |
| `emails/interview-invitation.php` | Einladung zum Vorstellungsgespraech |
| `emails/offer-letter.php` | Angebotsschreiben |
| `emails/rejection.php` | Absage an Bewerber |

## Template-Hierarchie

Das Plugin sucht Templates in dieser Reihenfolge:

1. `wp-content/themes/ihr-child-theme/recruiting-playbook/`
2. `wp-content/themes/ihr-parent-theme/recruiting-playbook/`
3. `wp-content/plugins/recruiting-playbook/templates/`

<Callout type="tip" title="Child-Theme nutzen">
Verwenden Sie immer ein Child-Theme fuer Template-Ueberschreibungen. So bleiben Ihre Anpassungen bei Theme-Updates erhalten.
</Callout>

## Beispiel: Stellenkarte anpassen

Kopieren Sie `partials/job-card.php` in Ihr Theme und passen Sie das HTML an:

```php
<?php
// wp-content/themes/ihr-theme/recruiting-playbook/partials/job-card.php

$job_id    = get_the_ID();
$location  = get_the_terms( $job_id, 'job_location' );
$type      = get_the_terms( $job_id, 'employment_type' );
?>
<div class="my-job-card">
    <h3>
        <a href="<?php the_permalink(); ?>">
            <?php the_title(); ?>
        </a>
    </h3>
    <?php if ( $location ) : ?>
        <span class="my-job-location">
            <?php echo esc_html( $location[0]->name ); ?>
        </span>
    <?php endif; ?>
    <?php if ( $type ) : ?>
        <span class="my-job-type">
            <?php echo esc_html( $type[0]->name ); ?>
        </span>
    <?php endif; ?>
    <?php the_excerpt(); ?>
</div>
```

## E-Mail-Templates anpassen

E-Mail-Templates verwenden das `base-layout.php` als Grundgeruest. Wenn Sie das Layout aendern moechten:

1. Kopieren Sie `emails/base-layout.php` in Ihr Theme
2. Passen Sie Header, Footer und Styling an
3. Die Platzhalter werden automatisch ersetzt

<Callout type="warning">
Aendern Sie in E-Mail-Templates keine Platzhalter-Variablen. Diese werden vom Plugin zur Laufzeit ersetzt. Die verfuegbaren Platzhalter finden Sie in der [E-Mail-Konfiguration](/docs/email).
</Callout>

## Formularfeld-Templates anpassen

Die Feld-Templates erhalten folgende Variablen:

```php
$field       // Array mit Feld-Konfiguration
$field_id    // Eindeutige ID des Feldes
$field_name  // Name fuer das Formular
$value       // Aktueller Wert (bei Bearbeitung)
$required    // Boolean - Pflichtfeld?
$options     // Array mit Optionen (bei Select, Radio, Checkbox)
```

<Callout type="info">
Formularfeld-Templates werden nur verwendet, wenn Sie den Form Builder (Pro) nutzen. Das Standard-Formular verwendet eigene Komponenten.
</Callout>
